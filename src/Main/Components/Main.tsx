import * as React from 'react';

interface IOwnProps {}

export class Main extends React.PureComponent<IOwnProps, {}> {
    render() {
        return (
            <p>Главная страниц</p>
        );
    }
}